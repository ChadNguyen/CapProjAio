<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Project AIO</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link active" aria-current="page" href="{{ url_for('main.index') }}">Home</a>
        <a class="nav-link" href="{{ url_for('main.about') }}">About</a>

        <!-- dropdown menu -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownGames" role="button" data-bs-toggle="dropdown"
            aria-haspopup="true" aria-expanded="false">
            Games
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdownGames">
            <a class="dropdown-item" href="{{ url_for('main.AnimalDices') }}">Animal Dices</a>
            {# <a class="dropdown-item" href="{{ url_for('main.game2') }}">Game 2</a> #}
            <!-- Add more games here as needed -->
          </div>
        </li>

        {% if current_user.is_active %}
        <a class="nav-link" href="{{ url_for('social.user',username=current_user.username)}}">Profile</a>
        <a class="nav-link" href="{{ url_for('social.post') }}">Post</a>
        <a class="nav-link" href="{{ url_for('auth.logout') }}">Sign out</a>

        {% else %}
        <a class="nav-link" href="{{ url_for('auth.register') }}">Register</a>
        <a class="nav-link" href="{{ url_for('auth.login') }}">Log In</a>
        {% endif %}
      </div>
    </div>
  </div>
</nav>